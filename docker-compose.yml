version: "3.8"

services:
  app:
    build:
      context: ./app
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
    command: >
      sh -c "uvicorn main:app --reload --host 0.0.0.0"
  
  ws-server:
    build:
      context: ./ws-server
    ports:
      - "8080:8080"